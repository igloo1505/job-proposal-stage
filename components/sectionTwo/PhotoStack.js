import React, { useState, useEffect } from "react";
import styles from "../../styles/photoStack.module.scss";

const PhotoStack = () => {
	return <div className={styles.photoStackContainer}>PhotoStack</div>;
};

export default PhotoStack;
